# SMC 扩刊执行全记录

本文件按时间顺序记录本项目中 Codex 执行的操作、得到的结果、偏差与决定。后续所有实质操作及结果持续追加到此文件。

## 2026-08-19：开始前的连接、阅读与设计确认

1. 使用 `ssh LXT@100.119.122.101` 测试远端连接。现有 ED25519 密钥认证成功，远端主机为 `DESKTOP-LPN6MT3`，账户为 `desktop-lpn6mt3\lxt`。
2. 在远端执行 `nvidia-smi`。确认 GPU 0 为 NVIDIA GeForce RTX 5090，驱动 610.88，显存 32607 MiB；探测时使用 747 MiB、利用率 0%、温度 46°C，CUDA UMD 显示 13.3。
3. 盘点本地仓库。当前分支为 `main`，跟踪 `origin/main`；初始提交为 `952986d Initial code upload`；`扩刊/` 当时尚未被 Git 跟踪。
4. 找到并完整阅读以下三份任务文档：
   - `扩刊/CODEX_PHASE1_FOUNDATION_PROMPT.md`（129 行）；
   - `扩刊/2026-08-19-smc-original-reproduction-design.md`（435 行）；
   - `扩刊/2026-08-19-smc-original-reproduction-plan.md`（1985 行）。
5. 确认本轮范围严格限定为 Gate 1 / Task 1–Task 5：可安装包、旧代码审计、typed schemas、确定性随机流、实例生成与实例银行、机器时间线、Weibull 可靠性、指标和 schedule validator。明确不实现规则、环境 `step`、reward、强化学习智能体或训练。
6. 确认 `code/`、`code1/`、`code2/` 必须保持不修改、不格式化、不移动、不删除。本地快速清单统计分别有 8、12、15 个受版本控制文件。
7. 探测远端基础工具。远端为 Windows PowerShell 5.1；`python` 仅解析到 Microsoft Store alias，`py`、Git、Conda、uv 均不在 PATH。结论：Python 3.11 与项目虚拟环境需要仅在 5090 主机安装。
8. 根据用户确认，采用本地 `扩刊/` 作为本阶段权威工作目录：新源码放入 `扩刊/original_repro/`，文档与审计记录放入 `扩刊/docs/`，实际运行产物保存在 `扩刊/` 的相应子目录；远端仅作为环境和计算执行端。
9. 读取并采用 Superpowers 工作流：`brainstorming`、`writing-plans`、`executing-plans`、`test-driven-development`、`using-git-worktrees`。既有设计稿和实施计划作为获批规格，不重新设计或扩大范围。

## 2026-08-19：正式执行开始

10. 用户明确授权直接开始、无需再次询问，并要求完成后把完整 `扩刊/` 文件夹上传到当前仓库对应的 GitHub 仓库。
11. 创建本文件 `扩刊/all.md`，从此持续记录所有操作与结果。
12. 完整读取 `executing-plans`、`test-driven-development`、`using-git-worktrees`、`subagent-driven-development` 及测试编写规范。决定按既有五项任务逐项执行、每项实施后进行规格与代码质量审查，并在最终阶段做整体审查。
13. 用户补充确认相关环境可以搭建在 5090。决定本地不创建项目虚拟环境；Python 3.11、官方 `torch==2.10.0` CUDA 12.8 wheel 及开发依赖只安装在远端 5090，环境信息和实验输出回传本地 `扩刊/`。
14. 检测 Git 工作区：仓库根目录为 `C:/Users/lwz20/Desktop/smc扩刊`，`.git` 与 common dir 相同，当前位于普通 `main` 检出，不是子模块或链接 worktree。用户已明确授权在当前目录执行，因此不创建额外 worktree。
15. 确认 Git 远端为 `origin = https://github.com/rayyyyyyyyb/smc1.git`（fetch/push），当前 `main` 跟踪 `origin/main`。
16. 创建计划专属、由自身 `.gitignore` 排除的 SDD 工作区：`.superpowers/sdd/2026-08-19-smc-original-reproduction-plan/`。
17. 对 Task 1–Task 5 做实施前一致性扫描并写入 SDD ledger。主要适配决定：所有新文件置于 `扩刊/`；远端 Windows 使用等价 PowerShell/Python 命令；旧代码审计显式指向仓库根；保留本地实例银行但不提交 1540 个压缩实例；五个功能提交之外如有必要增加一个仅含最终日志的收尾提交。
18. 将 Task 1 的 251 行原始计划内容提取为独立任务简报；记录实施前 Git 基线为 `952986db21d043f0f8e8bf82ad765061fa41cccf`。
19. 启动 Task 1 独立实施者 `/root/task1_implement`。明确要求：本地先写 `扩刊/original_repro/`；远端仅安装 Python 3.11、CUDA PyTorch 与开发依赖并执行测试；真实运行 CUDA 张量；回传 `hardware.json` 与 `environment.lock.txt`；不得修改 legacy 目录、`all.md` 或三份原始任务文档；不得推送 GitHub。
20. 等待 Task 1 期间首个 5 分钟窗口未收到完成消息；状态核对显示实施者仍在运行，未报告失败或阻塞，继续等待远端环境安装与验证。
21. 第二个 5 分钟等待窗口仍未收到完成消息；实施者状态仍为 running。发送非中断式进度请求，要求其在当前工具调用结束后说明处于 Python 安装、Torch 下载或测试中的哪一阶段，并继续执行。
22. Task 1 进度回报：本地已创建 `pyproject.toml`、README 与导入测试并同步到远端工作目录；远端 PowerShell 5.1 连接正常。Python 3.11 官方 `winget` 安装正在后台下载但尚未生成 `python.exe`，因此尚未进入 Torch 安装及 RED/GREEN 测试。
23. 后续回报显示 `winget` 已定位 Python 3.11.9，但 python.org 下载长时间无增长；此前仅下载 146838 字节即超时，重试仍慢。作出处理决定：停止无限等待，先用远端官方直链的 `curl.exe` 可续传或 BITS 下载并校验文件大小与 Authenticode 签名；若仍无进展，则本机下载同一官方安装包并通过 SSH 传输，安装和环境仍只留在 5090。
24. Task 1 下载回退进度：远端已停止卡住的 `winget` 并清理由本任务创建的 0-byte 缓存；远端 `curl` 遇到旧 partial 文件锁，遂启用本机对同一 Python 官方 URL 的可恢复下载。文件已增长到 19127110 字节且 TLS 连接持续活跃，完成后将通过 SCP 传输、在远端验证 Authenticode 签名并安装。另确认 5090 驱动 610.88、计算能力 12.0。
25. Python 安装包回退后的下一等待窗口未收到完成消息；实施者仍处于 running。已请求其在当前命令结束后报告 Python 安装状态及 Torch wheel 下载来源/速度，并继续执行。
26. Task 1 回报：Python 3.11.9 官方安装包已下载到 25762630 字节，接近完成但尚未安装；Torch 尚未开始，确定只从 `https://download.pytorch.org/whl/cu128` 获取。
27. 完整性检查发现下载文件无效：实际 27100366 字节，Python 官方目录标注正确大小 26216840 字节，`Get-AuthenticodeSignature` 返回 `NotSigned`。该文件未传输、未安装；授权仅删除这一确切的损坏临时文件。
28. 为避免约 20 KB/s 的同路径重下，决定改用 Astral 官方 uv 在 5090 上安装/管理 Python 3.11，并创建 `C:\Users\LXT\smc_gate1_env`；仅改变引导工具，不改变 Python 版本、远端环境位置或 PyTorch 官方 CUDA 12.8 来源。要求记录来源、命令和版本。
29. uv 回退连续两个 5 分钟等待窗口尚未返回完成事件，实施者状态均为 running。已要求在当前命令边界报告 uv 获取、Python 安装与 Torch 下载的分别进度，并规定任何单一无增长下载不得无限等待。
30. Task 1 关键进展：通过 Astral 官方 `install.ps1` 成功安装 uv 0.12.5；uv 成功安装 CPython 3.11.16；远端 `C:\Users\LXT\smc_gate1_env\Scripts\python.exe` 验证为 Python 3.11.16，`ensurepip` 提供 pip 24.0。官方 cu128 PyTorch 安装已启动，观测到 pip 缓存约 5223835 字节/59 个文件并继续增长。
31. PyTorch 安装确认严格使用 `https://download.pytorch.org/whl/cu128`，解析到 CPython 3.11 Windows 的 `torch 2.10.0+cu128`，主 wheel 大小 2867.4 MB；远端 Python 下载进程仍在运行，尚未完成安装。
32. PyTorch 下载后续观察：uv 进程 CPU 时间持续增加，日志仍指向官方 cu128 的 2.7 GiB torch wheel 及 sympy/networkx，缓存临时目录更新时间仍变化，因此尚不能判定卡死。实施者设置最后一个 60 秒有界观察；若无可见字节或完成信号则停止并报告网络传输阻塞。
33. 第二次 uv 重试使用 `UV_HTTP_TIMEOUT=120`，两个有界窗口仍无完成/字节日志；已知一次明确失败为 sympy 网络超时，torch 尚未安装。决定先采样 Windows 进程读写计数和最大临时文件长度；若增长则按 5 分钟检查点继续，若均不增长则仅停止该下载进程，从本机同一 `download.pytorch.org` 官方 URL 可续传下载精确的 `torch 2.10.0+cu128` CPython 3.11 win_amd64 wheel，校验 ZIP/metadata 后 SCP 到 5090 安装，禁止降级或第三方镜像。
34. 实际 I/O 采样确认下载并未卡死：远端 PID 6396 的 `WriteTransferCount` 在 60 秒内由 458536159 增至 607595394，增加 149059235 字节。uv 活动缓存未暴露为可枚举普通临时文件，但进程写入量满足继续条件，因此保留官方远端下载并按 5 分钟检查点观察。
35. PyTorch 官方下载首个 5 分钟检查点：`WriteTransferCount` 从 607595394 增至 1708086697 字节，约增加 1.10 GB，确认传输持续正常，继续保留下载。
36. 随后两个 5 分钟窗口均未收到完成事件，实施者仍为 running。已请求最新写入计数、torch import 状态以及是否进入依赖安装或 RED/GREEN 阶段，任务继续执行。
37. 最新远端 `WriteTransferCount=2853684460`，仍在增长并已接近 2867.4 MB 主 wheel 大小；`torch` 暂仍不可 import，uv 日志仍处于 wheel 与小依赖下载阶段，尚未进入项目 editable 依赖或 RED/GREEN。
38. 接近 wheel 完成后连续两个 5 分钟窗口无完成事件，但实施者仍为 running。要求其检查进程 I/O、uv/pip 存活状态、日志尾部、磁盘空间与 wheel 校验/解压状态，禁止在未诊断原因前盲目重启下载。
39. 诊断快照：uv PID 6396 仍存活，`WriteTransferCount` 从 3868507763 增至 3893276406（增加 24768643 字节）；速度明显下降但不是零进展。日志仍显示官方 cu128 torch 下载、sympy 已下载，未出现 timeout、磁盘、校验或解压错误，torch 尚不可 import；继续检查磁盘余量、活动网络连接和缓存临时文件。
40. 磁盘/连接诊断连续两个 5 分钟窗口仍未返回完成消息。要求实施者在当前工具边界报告磁盘可用空间、PID 网络连接、60 秒 I/O 增量和最新日志；若无连接且 I/O 零增长，执行已批准的本地官方 wheel 可续传回退。
41. 诊断结论：远端 C: 可用 1806380609536 字节（约 1.81 TB）；PID 6396 已无 TCP 连接；最近 60 秒写入仅增加 8055 字节，属实质停滞。日志显示 torch wheel 与 sympy 已 `Downloaded`，networkx 仍挂起，且无校验/解压错误。
42. 优化回退顺序：停止停滞进程但保留 uv 缓存；先从普通 PyPI 单独安装小依赖，再以 `--no-deps` 和官方 cu128 index 安装 `torch==2.10.0`，促使 uv 复用已完成的官方 torch 缓存。仅当缓存不完整并重新开始 2.87 GB 下载时，才启用本机可续传官方 wheel + SCP 方案。
43. 普通 PyPI 小依赖在远端仅约 0.18 MB/min；首次本机 `python -m pip download` 在 64 秒后退出且目标目录为空。只读检查本机解释器：默认 `python` 为 3.13.9，但 `py -3.11` 明确指向 `C:\Users\lwz20\AppData\Local\Programs\Python\Python311\python.exe`，本机 uv 位于 `C:\Users\lwz20\.local\bin\uv.exe`。
44. 决定用远端 uv 缓存离线、无依赖安装已完成的官方 torch wheel；普通依赖改由本机明确的 `py -3.11 -m pip download` 从官方 PyPI 下载 CPython 3.11 Windows wheels，构建 wheelhouse 后 SCP 到 5090，再使用 `--no-index --find-links` 安装。此举避免默认 Python 3.13 的解析偏差并保留依赖来源可审计性。
45. 实际执行远端 `uv pip install --offline --no-deps ... torch==2.10.0` 明确失败：`torch was not found in the cache`。结论是 uv 在终止未完成事务时清理了主 wheel，torch 仍不可 import；正式切换到已批准的本机 `py -3.11` 官方 wheel 可续传方案。
46. 本机回退命中精确构建：`torch-2.10.0+cu128-cp311-cp311-win_amd64.whl`（2867.4 MB），来源为官方 `https://download.pytorch.org/whl/cu128`；同时解析 filelock、typing-extensions、sympy、networkx、jinja2、fsspec、mpmath、MarkupSafe。本机下载进程 PID 39152 活跃。
47. 关键进展：远端最终从完成的 uv archive materialize 出 `torch 2.10.0+cu128` 与 `typing_extensions`；`import torch` 成功且 `torch.cuda.is_available()` 为 true。当前仅因 NumPy 未安装产生 warning。本机 CPython 3.11 wheelhouse 已完成依赖解析并进入普通依赖下载，观测到 NumPy 2.4.6 wheel（12.6 MB）下载，后续将用 wheelhouse 补齐依赖并消除 warning。
48. 本机普通 PyPI wheelhouse 仍卡在首个 12.6 MB NumPy，远端尚缺 pytest，严格 RED/GREEN 不能开始。只读盘点本机 Python 3.11：已有 fsspec 2026.6.0、Matplotlib 3.11.0、NumPy 2.4.6、pandas 3.0.3、PyYAML 6.0.3、SciPy 1.17.1、tqdm 4.67.3、typing_extensions 4.15.0；本机 uv 缓存约 3026296672 字节，但缺 pytest/ruff/mypy。
49. 为保持标准安装与完整包 metadata，决定停止并清理仅由本任务创建的未完成 wheelhouse，保留已安装的官方 torch；所有非 torch 普通依赖改从 TLS 验证的清华 PyPI HTTPS 镜像安装，严格遵守 pyproject 版本范围，并在 `environment.lock.txt` 记录解析版本。若该镜像也在有界窗口内零 I/O，则回退到本机 Python 3.11/uv 缓存 staging。
50. Task 1 完成并创建提交 `4b5597a build: add reproducible SMC package environment`。提交仅包含根 `.gitignore` 及 `扩刊/original_repro/` 下的 pyproject、README、两个 package initializer、硬件验证脚本和导入测试，共 7 个文件、141 行新增；三份原始任务文档和 `all.md` 保持未跟踪，等待最终归档提交。
51. Task 1 TDD：在缺少 package initializer 时远端导入测试 RED，错误为 `ModuleNotFoundError: No module named 'smc_repro'`；加入 initializer 后 GREEN，结果 `1 passed in 0.01s`。
52. Task 1 远端质量验证：`pytest -q` 为 1 passed；Ruff 为 `All checks passed!`；mypy 为 3 个源文件零问题；`pip check` 为无损坏依赖。
53. Task 1 硬件验证：Python 3.11.16；torch 2.10.0+cu128；CUDA runtime 12.8；NVIDIA GeForce RTX 5090；compute capability `[12,0]`；编译架构包含 `sm_120`；真实 CUDA 张量计算结果 14.0；无 unsupported-architecture warning。
54. 将远端产物回传到本地：`扩刊/original_repro/hardware.json`（1020 字节）和 `environment.lock.txt`（1550 字节），两者按计划被 Git 忽略。确认 `git diff --exit-code -- code code1 code2` 退出码为 0，legacy 目录未改变。
55. 环境来源记录：uv 0.12.5 通过 Astral 官方脚本安装并提供 CPython 3.11.16；torch 保持官方 cu128 artifact；普通项目/开发依赖和 torch 小依赖因官方 CDN 超时，经授权从 TLS 验证的清华 PyPI HTTPS 镜像安装。一个 75827200 字节的 partial wheel ZIP 校验失败，未被使用。
56. 基于 `952986d..4b5597a` 生成 Task 1 审查包（1 个提交，6625 字节），启动只读独立审查者 `/root/task1_review`，要求分别给出规格符合性与代码质量结论。
57. Task 1 独立审查结果：PASS / APPROVE。审查确认项目元数据、包结构、测试、README、`.gitignore` 与适配后的要求一致；硬件脚本确实检查 CUDA、设备、架构并执行张量计算；无禁止功能或 legacy 修改；提交范围正确。Critical、High、Medium、Low 问题均为 None。
58. 进入 Task 2，提取 182 行任务简报，记录基线 `4b5597aba94187320c989e284da4a83767a0bd24`。统计 Git 跟踪的 legacy 文件：`code/` 8 个、60594 字节；`code1/` 12 个、1174157 字节；`code2/` 15 个、997245 字节，共 35 个文件。
59. 启动 Task 2 独立实施者 `/root/task2_implement`：要求将设计稿逐字节复制到 `扩刊/docs/superpowers/specs/`，审计真实仓库根的三份 legacy 目录，测试先行并在 5090 的 Python 3.11 环境执行，生成 `扩刊/docs/audit/legacy_manifest.json`，仅提交 Task 2 文件且不得推送。
60. Task 2 实施者已先写测试，但误用本机 SSH 配置中的 `lwz`/`sh01-ssh.gpuhome.cc` 别名而连接超时。纠正其使用已验证的显式端点 `LXT@100.119.122.101`、Windows OpenSSH 路径、远端环境 `C:\Users\LXT\smc_gate1_env\Scripts\python.exe` 与工作区 `C:\Users\LXT\smc_gate1_work`，继续远端 RED/GREEN。
61. Task 2 完成并创建提交 `85416df chore: freeze legacy SMC snapshot`。新增 5 个文件、1174 行：规格副本、legacy manifest、审计脚本及两个测试文件。
62. Task 2 TDD RED：远端收集测试时因 `smc_repro.scripts.audit_legacy_outputs` 尚不存在而出现预期 `ModuleNotFoundError`；实现后 GREEN 为 `2 passed in 0.59s`。
63. 审计从同步到远端的真实仓库根生成，并与本地权威字节独立比对：manifest 记录 120 个实际文件，本地也为 120 个，mismatches=0，总大小 429922982 字节。数量多于 35 个 Git 跟踪文件是因为目录中存在预先已有的非跟踪文件；审计按要求覆盖所有真实文件。
64. 规格源文件与复制到 `扩刊/docs/superpowers/specs/` 的副本 SHA-256 均为 `F0297F62B9D0E4995A86EF6A454BFFAF3AC1E1EC304EDE46D1D1EE69E176CE39`，字节完全一致。源文件原有两处行尾空格也为保持字节证据而保留。
65. manifest 每条记录均包含非负 size、64 位小写 SHA-256 和 POSIX 风格路径，无 invalid entries。再次执行 `git diff --exit-code -- code code1 code2` 退出 0，确认 legacy 未被修改。
66. 基于 `4b5597a..85416df` 生成 Task 2 审查包（1 个提交，44807 字节），启动只读独立审查者 `/root/task2_review`，要求核对所有实际 legacy 文件、嵌套路径适配、字节相同规格副本和 TDD 证据。
67. Task 2 独立审查结果：Pass / Approved，无问题。确认单一提交仅新增 5 个范围内文件；120 个文件、429922982 字节的本地权威审计零缺失/零额外/零哈希不符；规格副本字节相同；审计脚本对缺失 legacy 目录明确抛出异常；嵌套 `扩刊/` 路径适配正确。
68. 进入 Task 3，提取 314 行任务简报，记录基线 `85416dfa5014e11efe91a85bd06a9b8115198e9e`。
69. 启动 Task 3 独立实施者 `/root/task3_implement`：要求所有测试先行并在 5090 执行 RED/GREEN，实现计划规定的 schemas、`set_global_seed` 与 blake2b `keyed_uniform`，并补充负 machine id、processing-time vector 长度不匹配两项强制边界测试；仅提交 Task 3 文件。
70. Task 3 初次实现创建提交 `083c99c feat: add typed scheduling schemas and deterministic streams`。聚焦测试 9 passed，Ruff 与 mypy clean，legacy diff clean；但提交前全套测试为 11 passed、1 failed，失败项是 `test_legacy_immutable` 的远端路径乱码，因此暂不审查、不进入 Task 4。
71. 按 `systematic-debugging` 读取错误和源码。定位到本地跟踪文件 `扩刊/original_repro/tests/test_legacy_immutable.py:21` 本身含错误乱码字面量 `"鎵╁垔"`，而非“扩刊”。曾尝试的两条远端补充探测分别因 PowerShell 管道被远端 cmd 解析、Python `-c` 引号被拆分而失败；但本地源码已直接证明坏值来源，无需依赖这两条失败探测。
72. 根因结论：Task 2 路径适配时把目录名错误转码并固化进测试源码；远端控制台又对该乱码二次呈现，造成不同乱码表象。单一修复假设是消除目录名字面量，令 `project_root = Path(__file__).resolve().parents[2]`、`repo_root = project_root.parent`。
73. 恢复原 Task 3 实施者执行修复：以已有全套失败作为 RED；只修改路径推导；在 5090 建立全 ASCII 但层级等价的 `repo/phase/original_repro` + `repo/code*` staging，排除 SSH/控制台编码；重跑失败项、全套测试、Task 3 聚焦测试、Ruff、mypy；把修复 amend 到原 Task 3 提交并更新报告。
74. 路径修复完成并 amend Task 3 提交，新 SHA 为 `76fc6da feat: add typed scheduling schemas and deterministic streams`。修复后源码不再含中文/乱码目录字面量，改为从测试文件位置结构推导 `project_root` 与 `repo_root`。
75. 修复验证在远端 ASCII staging `C:\Users\LXT\smc_gate1_work\repo\phase\...` 执行：此前失败项 1 passed；完整套件 12 passed in 4.56s；Task 3 聚焦测试 9 passed in 4.13s；Ruff All checks passed；mypy 2 个源文件零问题；legacy diff 仍为 0。
76. staging 期间一次递归传输超时留下 `sftp-server.exe` 占用临时 `PM.txt`；实施者确认该进程属于本次已中止传输后终止它，随后使用远端本地文件复制完成一次性 staging。该操作未触及本地 legacy 权威文件。
77. 基于 `85416df..76fc6da` 生成 Task 3 审查包（1 个提交，13242 字节），启动只读独立审查者 `/root/task3_review`，重点核对 schema API、随机流算法、两项补充边界测试及路径根因修复。
78. Task 3 独立审查结果：Pass / Approved，无问题。确认 frozen dataclasses、字段顺序/默认值、`IntervalType`、validation、half-open overlap 与计划一致；`keyed_uniform` 使用规定的 8-byte BLAKE2b 并保持 stateless；`set_global_seed` 显式设置全局流；两项额外边界测试存在；路径修复和全部验证证据有效。
79. 进入 Task 4，提取 593 行任务简报，记录基线 `76fc6da6dae3db4a38afb8f00e6ea039d5d2c7ac`。
80. 启动 Task 4 独立实施者 `/root/task4_implement`：严格 TDD 实现 generator、JSON-gzip I/O、bank builder；额外测试“合法 gzip 内损坏 JSON”；在 5090 生成两套完整 1540 条银行并逐字节比较 manifest；把一套 release bank 全量回传 `扩刊/original_repro/artifacts/banks/release/`，但本任务不提交生成实例或 release manifest。
81. Task 4 连续两个 5 分钟窗口尚未完成，实施者状态为 running。已请求其在当前命令边界报告 RED/GREEN、全套测试、bank A/B 生成/比较及 release bank 回传的分别进度，并继续有效运行。
82. Task 4 阶段快照：RED 为 2 个缺失 `instance_generator` 模块的预期收集错误；GREEN 本地/远端聚焦测试均 8 passed；Ruff clean；mypy 3 文件零问题；远端 ASCII staging 全套 20 passed。
83. 5090 上 bank A/B 均完整生成，各含 1540 个 `.json.gz`；两个 `manifest.json` 字节相同，SHA-256 为 `68a2fd2420a8710743b28db1b234b69dc2ecf96046d14950abac22cee7cd1515`。
84. release bank 已回传并验证到 `扩刊/original_repro/artifacts/banks/release/`，含 1540 个 gzip 和 1 个 manifest，总计 8083503 字节；临时传输包已清理，远端环境和两套生成银行保留。Task 4 五个源/测试文件已提交为 `7f0716a`，正在做最终报告和提交边界核验。
85. Task 4 正式完成提交 `7f0716a feat: add deterministic SMC instance banks`，提交 5 个文件、494 行。主会话复核本地 release bank：1540 个 gzip、gzip 总计 7653700 字节；manifest 存在、含 1540 个条目、SHA-256 与远端一致；没有任何 `.json.gz` 被 Git 跟踪，抽样文件命中根 `.gitignore` 第 20 行规则。
86. Task 4 详细验证：合法 gzip 内损坏 JSON 的额外测试已包含；聚焦测试 8 passed，完整套件 20 passed in 4.84s；Ruff clean；mypy 3 个源文件零问题；`git diff --check` 与 legacy diff 均 clean。首次用于创建 bank 的 Windows guarded shell 因 `IF EXIST` 作用域而没有执行，确认目录不存在后拆成独立命令，随后两次生成均成功。
87. 基于 `76fc6da..7f0716a` 生成 Task 4 审查包（1 个提交，19907 字节），启动只读独立审查者 `/root/task4_review`，重点核对 RNG 隔离、legacy 生成公式、gzip 确定性、异常语义及银行种子/顺序。
88. Task 4 独立审查结果：APPROVE，无 Critical/Important/Minor 问题。确认提交范围、legacy 公式与 metadata、局部 RNG、确定性 JSON/gzip、所有异常路径、scenario/seed 顺序及完整银行验证均满足规格；release artifacts 保持未提交。
89. 进入 Task 5，提取 585 行任务简报，记录基线 `7f0716ac69b63e5a1614d8013333dd5b1f3d4176`。
90. 启动 Task 5 独立实施者 `/root/task5_implement`：测试先行实现 timeline、Weibull、metrics、validator；保留所有边界测试；在 5090 跑 focused/full pytest、Ruff、mypy、compileall；生成并回传 `legacy_manifest_after_gate1.json` 与初始 manifest 逐字节比较；只提交 Task 5 文件。
91. Task 5 完成并提交 `425f6dc feat: add audited timeline reliability and metrics core`。RED 为 4 个目标模块尚不存在的预期收集错误；GREEN 为聚焦 11 passed。最终远端完整套件 31 passed in 4.66s；Ruff `All checks passed!`；mypy 对 13 个源文件零问题；compileall 退出 0；硬件再次验证 RTX 5090、torch 2.10.0+cu128、`sm_120` 与 CUDA 张量结果 14.0。
92. Gate 1 后 legacy manifest 已生成并回传。初始与 after manifest SHA-256 均为 `51bc6cd3ef0acdf260d8d0223f019f0c9deadad01fd306ea3b88c259b3cddd55`，本地主会话逐字节比较为 True，均含 120 个文件；`git diff --exit-code -- code code1 code2` 退出 0。
93. Task 5 提交新增 9 个计划文件并对先前 `audit_legacy_outputs.py` 删除一个多余空行，共 10 个文件、950 行新增、1 行删除。该额外一行变更只为使要求的全树 Ruff clean，不改变执行行为；已在 Task 5 报告中列为文件清单偏差。
94. 基于 `7f0716a..425f6dc` 生成 Task 5 审查包（1 个提交，41266 字节），启动只读独立审查者 `/root/task5_review`，核对公式、边界、validator 完整性、指标口径、after manifest 和额外空行清理。
95. Task 5 独立审查结果：Pass / APPROVE，无 Critical/Major/Minor 问题。确认可靠性、时间线、validator 与 metrics API/公式正确；所有边界测试齐全；验证证据有效；两份 manifest 与 legacy diff 正确；额外删除一个空行被认定为行为中立且可接受。
96. 五个功能任务的逐任务审查全部通过。读取并启用 `requesting-code-review` 及其 final code reviewer 模板，准备对初始基线至 Task 5 HEAD 的完整 Gate 1 变更做整体只读审查。
97. 生成完整 Gate 1 审查包 `952986d..425f6dc`，包含 5 个提交、122698 字节；启动最高能力档只读审查者 `/root/final_gate1_review`，提供设计、计划、执行提示、SDD rulings 及所有关键验收证据，要求独立检查计划对齐、正确性、确定性、错误处理、类型、测试、架构和范围。
98. 完整审查结论为 `Ready to merge: With fixes`。两个 Important：validator 未验证 PROCESS 实际 duration，零/过短区间可被当作完整排程；schemas、reliability、generator 输入和 JSON loader 未统一拒绝 NaN/Infinity，可能使非法实例/排程通过并产生 NaN 指标。
99. 三个 Minor：`MachineTimeline.intervals` 公共 list 可绕过 `add()`；缺少 validator 层面的机器 overlap 端到端测试；嵌套到 `扩刊/` 后 README 中 `../code*` 路径应为仓库根表述或 `../../code*`。审查同时建议硬件验证器可进一步主动拒绝非 5090/缺失架构，但该项不是阻塞 finding。
100. 修复裁定：PROCESS 区间在 schema 层必须为正 duration，validator 还必须拒绝短于所选机器 nominal processing time（允许未来 profile 用显式更长时间）；所有时间、duration、processing time、weight、Weibull 参数与生成器连续输入必须 finite；JSON 解析必须拒绝非标准 NaN/Infinity 常量。代价是先前被宽松接受的非有限输入将明确失败。
101. 修复裁定：将 `MachineTimeline` 内部存储私有化并以只读 tuple 暴露，添加 validator overlap 集成测试，修正 README 路径。代价是任何未规划的外部代码若直接 append `timeline.intervals` 将需要改用 `add()`；Gate 1 尚无此类消费者。
102. 为保持任务提示要求的五提交结构，集中修复将 amend 当前第五个提交，而非新增功能提交；随后针对旧/新 Task 5 HEAD 生成 scoped fix diff 并做一次独立复审。
103. 启动集中修复实施者 `/root/final_gate1_fix`，完整交付五项审查 finding 与既定裁定；要求严格执行回归测试 RED/GREEN，在 RTX 5090 重新跑完整测试、静态检查、硬件验证、双实例银行重建对比和 legacy 审计，最后 amend 第五个功能提交且不得推送或修改本日志。
104. 在等待修复期间复核本地 release bank 与仓库连接：release 目录仍有 1540 个 gzip（共 7653700 字节）和 429803 字节的 manifest；manifest 的 `files` 为 1540 项，SHA-256 仍为 `68a2fd2420a8710743b28db1b234b69dc2ecf96046d14950abac22cee7cd1515`。抽样 gzip 命中 `.gitignore` 第 20 行且没有 gzip 被 Git 跟踪；`origin` 的 fetch/push 均为 `https://github.com/rayyyyyyyyb/smc1.git`。`git fsck` 仅报告一个不影响当前引用的 dangling tree `f3123429...`，没有对象损坏。
105. 通过 `git ls-remote --heads origin main` 只读检查 GitHub：远端 `main` 仍指向初始提交 `952986db21d043f0f8e8bf82ad765061fa41cccf`，没有外部并发推进，后续可按既定五功能提交加日志归档提交安全推送。
106. 集中修复阶段回报：5090 上获得有效 RED，共 83 个聚焦用例中 57 failed、26 passed，失败精确覆盖 finite 数值、PROCESS duration、timeline 封装和严格 JSON；既有 overlap 与允许更长 PROCESS duration 的 characterization 已通过。最小实现后首轮聚焦 GREEN 为 83/83，mypy clean；Ruff 仅发现一处 102 字符长行并已本地折行。另增加 timeline 对非有限输入的范围内回归，远端先得到预期 6/6 RED，正在实施相应最小修复；此时尚未跑最终完整套件、双 bank、legacy 审计或 amend。
107. 集中修复完成并 amend Task 5，新 SHA 为 `7be1315a5bbf734961bcb6a7270841131775b175`，分支仍恰好包含五个功能提交且未推送。最终聚焦 GREEN 为 89 passed；5090 新鲜完整门禁为 100 passed in 4.91s、Ruff clean、mypy 对 13 个源文件零问题、compileall 退出 0；硬件为 RTX 5090 / `sm_120` / torch 2.10.0+cu128 / CUDA 12.8，真实 CUDA 结果 14.0。新建 bank A/B 各 1540 个 gzip、各 1540 项 manifest，manifest 字节相同且 SHA-256 仍为 `68a2fd2420a8710743b28db1b234b69dc2ecf96046d14950abac22cee7cd1515`。legacy 初始/final-fix manifest 字节相同、均为 120 文件，SHA-256 为 `51bc6cd3ef0acdf260d8d0223f019f0c9deadad01fd306ea3b88c259b3cddd55`，冻结目录 diff 退出 0。修复详细报告保存在忽略的 SDD 工作区 `final-fix-report.md`。
108. 主会话检查旧 Task 5 `425f6dc..7be1315`：共修改 13 个范围内文件，新增 276 行、删除 13 行，`git diff --check` 无错误；当前 `main` 相对 `origin/main` ahead 5，工作区除三份原始任务文档、`all.md` 和 release artifacts 外没有未提交内容。
109. 按子代理开发流程启动唯一一次 scoped 只读复审 `/root/final_gate1_rereview`，直接比较旧/新 Task 5 快照 `425f6dc..7be1315`；要求逐项裁定原审查的两个 Important 与三个 Minor、检查真实测试覆盖及新回归，并以 `APPROVE` 或 `BLOCK` 给出最终结论，不得编辑、提交或推送。
110. scoped 独立复审结论为 `APPROVE`，没有 Critical、Important 或 Minor 问题。复审逐项确认：PROCESS 正时长、nominal 下限/容差/允许更长均已实现并测试；schemas、reliability、generator、timeline 和 JSON 边界均拒绝 NaN/Infinity；时间线构造输入经 `add()` 校验且只读暴露；validator 两类机器重叠端到端测试齐全；README 路径已修正。审查者还独立在 5090 得到聚焦 92 passed、完整 100 passed in 4.86s，确认五功能提交结构、Gate 1 范围及三个 legacy 目录不变。
111. 完整读取并启用 `verification-before-completion` 与 `finishing-a-development-branch` 做最终收口；另在主会话首次新鲜验证遇到意外收集错误后，完整读取并启用 `systematic-debugging`。由于用户已明确要求把完整 `扩刊/` 上传对应 GitHub 且不再询问，分支集成选择已预先确定为直接推送当前 `main`，不再重复弹出选项。
112. 主会话第一次用远端 `cmd.exe` 设置 `PYTHONPATH` 后运行完整 pytest，出现 6 个收集错误：`instance_generator`、`metrics`、`reliability`、`timeline`、`validator` 等模块找不到。错误路径表明 Python 仍解析到旧 editable 安装位置；这次失败属于 SSH/cmd 命令传输层未正确传播 staging `PYTHONPATH`，不是当前代码测试失败，因此未修改代码。
113. 根因诊断采用 PowerShell `-EncodedCommand` 单变量实验：远端实际 `PYTHONPATH` 为 `C:\Users\LXT\smc_gate1_work\repo\phase\original_repro\src`，`sys.path` 第二项精确为该目录，`smc_repro.__file__` 也解析到 staging 的 `src\smc_repro\__init__.py`。由此确认编码 PowerShell 是可靠调用方式，后续所有主会话门禁均使用它。
114. 主会话按可靠调用方式重新执行 5090 完整门禁：pytest `100 passed in 5.17s`；Ruff `All checks passed!`；mypy 对 13 个源文件零问题；compileall 退出 0。硬件验证输出 Python 3.11.16、torch 2.10.0+cu128、CUDA runtime 12.8、NVIDIA GeForce RTX 5090、compute capability 12.0、编译架构含/设备架构为 `sm_120`，真实 CUDA 张量结果 14.0。
115. 主会话重新读取 final-fix bank A/B：两者各 1540 个 `.json.gz`、manifest 各 1540 项，两个 manifest 逐字节相同；双方 SHA-256 均为 `68a2fd2420a8710743b28db1b234b69dc2ecf96046d14950abac22cee7cd1515`，所有断言退出 0。
116. 首个 legacy 数量校验脚本错误地把 manifest 的 `files` 对象当数组计数，虽已得到字节相同和双方哈希一致，却误报数量为 1 并主动失败。只读检查确认 `files` 实际为以相对路径为属性名的 JSON object；改用 `PSObject.Properties` 后重跑通过：initial/final-fix 均 120 个文件、manifest 字节相同，双方 SHA-256 均为 `51bc6cd3ef0acdf260d8d0223f019f0c9deadad01fd306ea3b88c259b3cddd55`。该过程未修改产物。
117. 本地主会话最终 Git/产物门禁退出 0：`git diff --check` clean；`git diff --exit-code -- code code1 code2` clean；相对 `origin/main` 恰好五个功能提交、31 个变更文件、legacy 变更 0；Git 跟踪的 gzip 为 0。两份本地 legacy manifest 字节相同、均 120 文件且哈希与远端一致；本地 release bank 仍为 1540 个 gzip/1540 项 manifest、哈希与远端一致；当前源包有 13 个 Python 文件。
118. 推送前再次读取 GitHub `main`，仍为预期基线 `952986db21d043f0f8e8bf82ad765061fa41cccf`；`git push --dry-run origin main` 成功预演 `952986d..7be1315 main -> main`，证明没有远端并发推进或认证阻塞。下一步先实际推送五个已验收功能提交，再把该推送结果连同三份原始文档、`all.md` 与评审后 release manifest 做日志归档提交并二次推送。
119. 已执行第一次实际 `git push origin main`，GitHub 返回 `952986d..7be1315 main -> main`；随后用 `git ls-remote` 复核远端 `main` 精确指向 `7be1315a5bbf734961bcb6a7270841131775b175`。五个功能提交已上传成功。
120. 收尾归档边界按计划执行：将三份用户提供的 Gate 1 Markdown、`all.md` 和通过评审后的 release `manifest.json` 纳入一个纯文档/证据提交；1540 个 `.json.gz`、`hardware.json`、`environment.lock.txt` 与远端虚拟环境继续按计划受 `.gitignore` 保护，不上传 GitHub。gzip 全量产物仍完整保存在本地 `扩刊/original_repro/artifacts/banks/release/` 和 5090 上，避免把大批生成数据写入仓库历史。
121. 精确暂存上述五个归档文件后核验：暂存区仅有三份原始 Markdown、`all.md`、release `manifest.json`，共 13833 行；没有 `.json.gz`。`git diff --cached --check` 仅报告设计稿第 3、4 行的尾随两个空格；只读检查确认这两处是用户原始 Markdown 的强制换行语法，因此保留原始文档字节，不进行“修复”。五个功能提交的生产代码差异此前已单独通过 `git diff --check`。
122. 归档提交完成后将再次检查提交文件清单、工作区、远端分支并推送 `main`；这是日志可纳入同一不可自指提交的最后记录边界。最终归档提交 SHA 和第二次推送的实际 GitHub 结果将在任务交付消息中报告。

## 2026-08-20：下一阶段检查与预实验实施

123. 用户要求读取新加入 `扩刊/` 的检查与下阶段指导，在本地完成本阶段任务和代码、在 RTX 5090 搭建/复用运行环境、持续按顺序记录全部动作与结果，最后把完整 `扩刊/` 重新推送到同一 GitHub 仓库；用户明确授权直接开始且不再询问。
124. 完整读取 `using-superpowers` 及其 Codex 工具适配说明，并读取 `brainstorming`。本轮被归类为已有正式审计/实施计划驱动的架构性阶段；三份新文件视为用户已批准的设计与范围依据，因此遵从“不再询问”跳过重复审批，只在读取后做一致性扫描和执行计划。
125. 首次只读盘点：本地 `main` 与 `origin/main` 同步，HEAD 为 `437c254 docs: archive Gate 1 execution record`；工作区仅有三个新文件未跟踪，分别是 `2026-08-20-smc-gate1-audit-report.md`（21623 字节）、`2026-08-20-smc-preexperiment-implementation-plan.md`（131451 字节）和 `CODEX_PHASE2_PREEXPERIMENT_PROMPT.md`（15778 字节）。用户称“三个文件夹”，实际文件系统中是三个 Markdown 文件；现有 `docs/`、`original_repro/` 目录及 Gate 1 文件保持原状。
126. 计算并记录三份新文件：外部审计 572 行、SHA-256 `d448b62bf7d6294ce851b6ecb735a13ba98ac54565ef194dc773c0fcde33c94e`；实施计划 3747 行、SHA-256 `7bb6f65014bb8bbd867b8246fe7c6e0b2b2ddcda09219f0ae7411a2d7ea7680f`；Phase 2 提示 472 行、SHA-256 `521a6d2e0075a2c4ef271ca7e92cdda79abbf7185c8d39098816252f8a556925`。随后逐行完整读取三者，无截断跳读。
127. 读取后的范围结论：执行 Task 0–Task 9，依次关闭 clean-clone/legacy 审计、随机键/metadata/区间、指标/validator、1540 实例银行与环境快照、三 profile、命名状态/奖励、规则库、构造式环境、深度/表格智能体与 checkpoint、最终 preflight/clean-worktree；严禁正式五种子训练、540 实例全评估、批量调度、统计/绘图、外部 benchmark、GNN/PPO 或修改 `code*`。
128. 完整读取 `executing-plans`、`using-git-worktrees`、`test-driven-development` 及测试编写规范；计划明确要求有子代理时优先 `subagent-driven-development`，因此又完整读取该技能。执行方式锁定为每个 Task 严格 RED→GREEN→聚焦提交→独立任务审查，全部完成后做整分支审查、5090 preflight 和 GitHub 推送。
129. 完整阅读当前 `扩刊/original_repro/src` 与 `tests`：25 个文件、1648 行、57695 字节。首次用于统计文件的 PowerShell `foreach` 后直接管道写法触发 `An empty pipe element is not allowed`，未产生任何文件变更；改为先构造 `$rows` 后成功。当前源码/测试的已知边界与审计报告一致：legacy audit 扫全目录、keyed RNG 使用分隔符拼接、metadata 仍为可变 dict、非 PROCESS/零搜索时长仍允许、metrics/validator 缺本轮语义加固。
130. 按隔离要求检测 Git：当前为普通 checkout（`.git` 与 common dir 相同）、非子模块、起始分支 `main`、起始完整 HEAD `437c2544f8d91633e4f85c3322e006af526aa420`。创建并切换到隔离分支 `preexperiment-readiness-20260820`；三个新 Markdown 和本日志修改随工作区保留，尚未提交。
131. 尝试运行 SDD 自带 `sdd-workspace` Bash helper 失败，原因是本机 `bash.exe` 指向未安装的 WSL；未修改生产文件。随后按脚本等价逻辑创建 `.superpowers/sdd/2026-08-20-smc-preexperiment-implementation-plan/`，现有 `.superpowers/sdd/.gitignore` 的 `*` 已由 `git check-ignore` 验证覆盖该目录；创建 `progress.md`，记录起始 SHA、隔离分支、远端环境、Task 0–9 状态及两项可恢复裁定。
132. 补读三套 legacy 实现时，首次把 `test_runner.py` 误写为 `扩刊/code2/test_runner.py`，PowerShell 明确报告路径不存在；目录复核确认 `code/`、`code1/`、`code2/` 位于仓库根而不是 `扩刊/`，没有产生文件变更。随后完整读取 `code2/test_runner.py`，并对已完整读取的 `code2/DQN.py`、`Job_shop.py`、`test_runner.py` 分别与 `code/`、`code1/` 做逐行 `git diff --no-index`。确认 `code` 变体主要提供固定 Rule1–Rule9 对比、删除 reward3；`code1` 变体主要提供开启/关闭预防性维护的消融、补充全部作业的 TR 口径、删除 reward3；`code2` 保留 Double/Vanilla DQN 开关、reward3 与单策略场景图。三套差异只读核验完成，未修改任何 legacy 文件。
133. 完成 Task 0–Task 9 的实施前一致性扫描：锁定 tracked legacy manifest/clean-clone、typed RNG/metadata/timeline、metrics/validator、bank/environment snapshot、profile config、observations/rewards、rule views、constructive environment/contracts、agents/checkpoints、最终 preflight 之间的生产者—消费者接口；确认生成的 bank、checkpoint 与 preflight 产物继续由显式 ignore 保护，而可移植 manifest、环境快照、配置、测试、README 和证据记录必须纳入 Git。对源文档实际位于 `扩刊/`、smoke 配置是严格覆盖层、Task 2 维护时长 fixture 需随语义更新、Task 7 候选估计不抽取维护随机数、Task 9 clean gate 与 ignored 产物并存等边界作出不扩大范围的执行裁定，并写入忽略的 SDD ledger。
134. 通过编码 PowerShell SSH 只读核验 5090：主机仍为 `DESKTOP-LPN6MT3`，远端 `git` 仍不在 PATH；复用环境 `C:\Users\LXT\smc_gate1_env` 的 Python 3.11.16 与 torch 2.10.0+cu128，`torch.cuda.is_available()` 为 true，GPU 为 NVIDIA GeForce RTX 5090，compute capability 为 `(12, 0)`；Gate 1 staging 存在并含 12 个测试文件。未重装或改动 torch 环境。
135. 在5090现有 Gate 1 staging 中显式设置 `PYTHONPATH`、`PYTHONHASHSEED=0`、`CUBLAS_WORKSPACE_CONFIG=:4096:8` 后运行本阶段实施前基线：完整 pytest 为 `100 passed in 6.22s`，Ruff 为 `All checks passed!`，mypy 对 13 个源文件零问题，compileall 退出码 0。该结果证明开始 Task 0 前远端环境和 Gate 1 基线健康。
136. Task 0 按 TDD 完成首轮实现：旧 HEAD 的纯 Git 跟踪快照在5090对 legacy equality 得到预期 `1 failed`；新增 tracked-scope 规格在实现前得到 `3 failed`，实现后聚焦 `3 passed`。提交前5090完整套件为 `101 passed`、Ruff clean、mypy clean、compileall 0；建立首轮单提交 `169620252d13b0ad9725efdac8cf6a671abf2dca`。提交后的新鲜跟踪快照含 77 个提交文件、35 个 tracked legacy 文件，RTX 5090 完整套件 `101 passed in 4.72s`。两份重命名历史 manifest 仍各含120项且字节相同，SHA-256 均为 `51bc6cd3ef0acdf260d8d0223f019f0c9deadad01fd306ea3b88c259b3cddd55`；首轮 tracked manifest 含35项。三套 legacy 本地目录的聚合哈希前后不变，未推送。
137. 完整读取并采用 `requesting-code-review`，派出只读独立审查者逐行检查 `437c254..1696202`、Task 0 规格、实现、测试和 manifest，而不是只依赖实施报告。审查确认提交边界、两份 R100 历史 manifest、文档逐字复制、35 个 tracked 路径计数和基本 API/CLI 正确，但裁定 Task 0 暂不能放行。
138. 独立审查发现跨平台 Critical：tracked audit 当前读取工作树字节，且仓库没有固定 checkout EOL；提交 manifest 来自 Windows CRLF 工作树，而 Git blob 为 LF，因此 Linux 或 `core.autocrlf=false` 的干净 clone 会失败。审查还发现 Important：`Path.write_text()` 在 Windows 对 JSON 换行做平台转换，报告的工作树文件 SHA 与仓库中的规范 blob SHA 不一致，artifact 本身不具字节级确定性。审查建议改为规范 Git/index blob 字节、另行验证 legacy 工作树无语义差异、显式写入 LF，并覆盖 CRLF/LF 双配置。
139. 完整读取并采用 `receiving-code-review` 后做独立事实核验：本机 `core.autocrlf=true`，当前35个 manifest 项中有23个工作树 SHA 与已提交 manifest 期望值不同，而 `git diff --exit-code -- code code1 code2` 仍为0，证实不是 reviewer 误判。已把两项问题、严格 RED/GREEN、双 EOL 回归、重新生成 manifest、更新报告以及 amend 同一 Task 0 提交的要求发回原实施者；在复修和复审通过前不进入 Task 1。
140. Task 0 复修先在5090得到审查回归 RED：`2 failed, 2 passed`，分别捕获 CRLF 序列化和 LF/CRLF 工作树字节差异。最小修复后，TRACKED scope 由 `git ls-files -z` 枚举并通过 `git cat-file blob :path` 哈希规范 index blob；ALL_LOCAL 仍读取真实本地文件；manifest 改为二进制 UTF-8 加显式单个 LF；immutable test 另行执行 `git diff --exit-code -- code code1 code2` 防止本地 legacy 语义修改。聚焦 GREEN 为 `4 passed`，完整5090套件为 `102 passed`，Ruff、mypy、compileall 均通过。
141. 同一 Task 0 提交已 amend 为 `36e4a6ee205f9f6c7e37ecdbf51b82cc4e849588`，仍是相对 `437c254` 的唯一一个提交且未推送。CRLF/LF 两个新鲜 clean snapshot 的 DQN 工作树字节长度分别为20012/19519，但规范 tree ID 同为 `d3c2598fd95caff8176a5d103c75878f88c1df86`，双方在 RTX 5090 均通过102个测试。最终 tracked manifest 含35项，SHA-256 `707eeaef6937d6f697854f5d894ce678a83188d0eb9cf15b21f165e78b43764d`，HEAD/index/工作树字节相同、无 CRLF、末尾为 LF、0 个 index blob 不匹配；legacy 聚合哈希不变。原独立审查者复审确认先前两项 finding 全部解决，Critical/Important/Minor 均为零并给出 `Ready to proceed: Yes`，Task 0 正式完成。
142. Task 1 严格分三段执行 TDD：随机键规格先得 `4 failed, 4 passed`，实现 typed length-prefixed BLAKE2b key encoding 后为 `8 passed`；metadata 防御性复制/只读/JSON scalar/finite 规格先得 `2 failed, 47 passed`，实现后为 `49 passed`；非 PROCESS 区间和 timeline 查询正时长规格先得 `4 failed, 54 passed`，实现后为 `58 passed`。最终聚焦套件 `74 passed`、完整套件 `112 passed`，5090 上 Ruff、mypy（13个源文件）和 compileall 均通过；release bank manifest SHA-256 保持 `68a2fd2420a8710743b28db1b234b69dc2ecf96046d14950abac22cee7cd1515`。
143. Task 1 建立单提交 `6edac6521519618f1294d46f0e58c1025fae4c65`（`fix: harden deterministic streams and immutable schemas`），仅修改 `seeding.py`、`schemas.py`、`instance_io.py`、`timeline.py` 及三个对应测试文件；未修改 legacy 目录或任何 legacy/bank manifest，未推送。独立只读审查验证 typed key 的分隔/类型/有限值规则、两类 metadata 运行时冻结与 JSON round-trip、所有 interval/timeline 正时长约束及调用者兼容性，并额外完成2379个碰撞导向 encoding probes；Critical/Important/Minor 均为空，`Ready to proceed: Yes`，Task 1 正式完成。
144. Task 2 实施者首次在本机账户路径查找 `C:\Users\LXT\smc_gate1_env`，因该环境实际位于远端5090而报告不存在；此次查找没有执行或宣称 RED，只已添加第一阶段 metrics 测试，未改生产实现。主控制器明确纠正执行边界：通过 `ssh LXT@100.119.122.101` 的 encoded PowerShell，把当前项目同步到新的有界 Task 2 staging，在远端 `C:\Users\LXT\smc_gate1_env\Scripts\python.exe` 设置三项确定性环境变量后继续严格 RED/GREEN；禁止改用本地 Anaconda 或重装任何环境。
145. Task 2 首轮 TDD 在5090得到 metrics `6 failed` 与 validator `6 failed, 12 passed` 的有效 RED；实现 horizon clipping、19字段 `ScheduleMetrics`、新增指标、SETUP/PM/CM 机器语义和最终 PROCESS horizon 后，metrics `6 passed`、validator `18 passed`、聚焦 `24 passed`、完整套件 `127 passed in 6.21s`，Ruff/mypy/compileall 均通过。首轮单提交为 `b3d85a436a63b537184e984acbcd797a7fa2bd2d`，仅含 metrics/validator 及对应测试，未推送。
146. Task 2 独立只读审查确认字段顺序、所有时长在 `[0,makespan]` 内裁剪、on-time/flow/downtime/failure 计算、维护时长容差和 complete-only final horizon 逻辑大体正确，但发现一个 Important 数值边界：`available_capacity <= 0` 被静默改成 availability-adjusted utilization 0.0，从而绕过“非法利用率必须显式报错”的规格。代码核验确认该分支存在，且 schema/validator 的 `1e-9` overlap 容差允许用更小的正时长构造此合法输入边界。已要求原实施者先新增能命中该边界的 RED，再仅把非正 denominator 改为 `ValueError`，不改变 overlap 容差或扩大范围，随后 amend 同一 Task 2 提交并重跑全部5090门禁。
147. Task 2 审查修复先得到 `1 failed`（预期 `ValueError` 未抛出）的有效 RED，修复后该回归 `1 passed`、聚焦套件 `25 passed`、完整套件 `128 passed`，5090上 Ruff、mypy、compileall 均通过；`available_capacity <= 0` 现显式抛错。首轮提交 amend 为 `c03e15322ca06e3d5e677191a609ae4d14010d2d`，仍只有一个 Task 2 提交且未推送。原审查者复审验证异常边界与正常利用率1.0均正确、validator 和各容差无附带改动、protected paths/manifests 不变，Critical/Important/Minor 均为空并给出 `Ready to proceed: Yes`，Task 2 正式完成。
148. Task 3 新实施者先按 brief 等待 Task 2 放行；主控制器显式确认 `c03e153...` 已独立复审通过。随后实施者在本机发现4070且短名 `ssh LXT` 无法解析，未据此执行任何正式测试或环境变更；主控制器再次明确远端连接为 `C:\Windows\System32\OpenSSH\ssh.exe LXT@100.119.122.101`、认证已配置、远端命令使用 UTF-16LE Base64 的非交互 PowerShell、传输使用系统 `scp.exe`、环境位于远端 `C:\Users\LXT\smc_gate1_env`，禁止使用本机4070并继续完整 Task 3。
149. Task 3 首轮 bank verifier TDD 在5090得到 `9 failed in 0.66s`（均因 verifier 模块尚不存在）的有效 RED，实现后为 `9 passed`；首轮完整套件 `137 passed`，Ruff、mypy（15个源文件）和 compileall 通过。全量 bank 在5090生成并验证 expected=verified=1540、无额外 gzip、生成/参考 manifest 字节相同且 SHA-256 为固定值 `68a2fd2420a8710743b28db1b234b69dc2ecf96046d14950abac22cee7cd1515`；临时回传目录先在本地严格验证，再规范化到忽略的 `artifacts/banks/materialized/`，本地最终为1540 gzip加manifest。忽略的 preflight report 也回传本地。
150. Task 3 在真实5090生成 portable environment snapshot：GPU `NVIDIA GeForce RTX 5090`、compute capability 12.0、torch 2.10.0+cu128、CUDA runtime 12.8、真实张量结果14.0、41个排序包记录，绝对editable路径/敏感值扫描通过。首轮单提交为 `046ad0378a88dcfebfaf4b9aeee615cbe879a4ad`，含 verifier、environment capture、测试、README和环境JSON，未推送；本地完整 materialized bank和preflight继续由忽略规则保留。
151. Task 3 独立只读安全/可移植性审查确认本地1540文件内容、确定性gzip头、环境快照和README基本正确，但给出三项 Important：固定 manifest SHA 实际依赖Windows CRLF，Git blob/Linux builder为LF，clean Bash不能复现；manifest出现“合法项在前、危险项在后”时会在完成全表路径验证前读取首个实例；root/schema/canonical path 校验不足，可接受schema 2、漏判 `C:/`、把 `safe` 与 `./safe` 别名计成 expected=2/verified=1 却返回ok。另有一项 Minor：materialized manifest和preflight report只在本地 `.git/info/exclude` 忽略。已要求原实施者先增加上述回归RED，再添加精确 `.gitattributes` 保持固定CRLF SHA、builder显式CRLF、全表分阶段路径/containment/symlink/collision校验、严格schema/SHA/count语义、tracked `.gitignore`，并在本地重新生成带本地路径的preflight；随后amend同一Task 3提交并做LF/CRLF clean-checkout与5090全门禁复验。
152. Task 3 审查修复先得到 `10 failed, 12 passed` 的有效 RED；修复后 verifier 聚焦 `22 passed`、5090完整套件 `150 passed`，Ruff、mypy、compileall 均通过。新增 `.gitattributes` 仅对 release manifest 强制 `text eol=crlf`，builder 显式写CRLF UTF-8；release blob `add55c9236eab4343734f96a5fa75611982f2012` 未改变。在 `core.autocrlf=false` clean clone 中重建1540文件后 reference/generated 均为429803字节、SHA `68a2fd...1515` 且字节相同。verifier 现分为完整 lexical validation、全部 resolve/collision/containment 校验、最后 content verification 三阶段；晚出现危险路径、非object/schema2、Windows drive、非规范路径、casefold/resolve别名、错误SHA、外逃symlink/junction均有回归。tracked `.gitignore` 覆盖 materialized/preflight，本地 preflight 重生为1540/1540且路径指向现存本地产物。提交 amend 为 `ba4ec24d7ea3c850b36874c41c5ad7749539082d`，仍是唯一 Task 3 提交、未推送；复审 Critical/Important/Minor 全空，`Ready to proceed: Yes`，Task 3 正式完成。
153. Task 4 严格配置首轮 TDD 在5090 collection阶段得到预期 `ModuleNotFoundError: smc_repro.config`、exit 2 的 RED；实现三种 scientific profile、smoke override、strict dataclasses/validation 和 A-001～A-012 后，聚焦 `90 passed`、完整套件 `240 passed`，Ruff、mypy（16个源文件）、compileall 均通过。profile SHA 分别为 legacy `622109cb3bbd706356b954987ef091e88b6d9fc0c1ef7b480e0ba978f63e0714`、paper `9f80c97c8876b4fec3b73e457dfd42adf7fdba0709821194847a420d8c8b1104`、corrected `8b9bf5e8f9a06a1b2e197a6cbaaf8a0c2070bf9b5c1632c7c0ba40b868833708`；smoke 被验证为 paper_repro 的严格覆盖而不是第四profile。首轮单提交为 `6e9601dc25fd9f59daf2726bbc038f98909a6b71`，未推送。
154. Task 4 独立只读审查逐字确认 ambiguities JSON、四份YAML、七个enum、冻结dataclass、所有现有未知/缺失/类型/范围/维度/urgency/override规则和三SHA均正确，但发现一项 Important：`yaml.safe_load()` 默认静默接受重复键，根级冲突schema、重复section和嵌套 `training.episodes` 都以后值覆盖前值，这会隐藏科学配置篡改/误写并违反strict-loader。已要求原实施者先为根级、嵌套级和smoke override重复键增加RED，再使用严格SafeLoader在任何mapping深度构造前拒绝重复键并提供key及行列上下文，保持YAML字节、所有profile SHA/API不变，随后amend同一Task 4提交并重跑5090门禁。
155. Task 4 重复键修复先得到5个重复键用例失败的RED，实现严格SafeLoader后 duplicate-focused `5 passed`、聚焦95、完整245，Ruff/mypy/compileall通过；错误包含文件、YAML路径、重复键及两处行列，根/嵌套/override/同值/冲突值均拒绝，普通alias与merge key保留。提交首次amend为 `42877f53...`，三profile SHA与smoke语义不变。复审随后发现一个Minor：cyclic alias令预扫描产生裸 `RecursionError`；再次增加回归得到 `1 failed`（另3个alias兼容用例通过），用per-load node identity visited集合修复后4个alias用例全通过，循环 `upper_hidden` 现以 `$.architecture.upper_hidden[0]: expected an integer` 正常拒绝，重复检测未受损。
156. Task 4 最终提交为 `d5c629362e59f0eebf0f5bb9f66fb60fac5081ea`，仍是唯一Task 4提交且未推送；最终5090聚焦99、完整249，Ruff/mypy/compileall通过，三SHA和所有配置/ambiguity字节不变。独立审查者第三轮验证 cyclic alias、重复alias值、普通alias/merge、protected paths 和 fresh full suite，Critical/Important/Minor 全空并给出 `Ready to proceed: Yes`，Task 4 正式完成。
157. Task 5 首轮TDD在5090因 `smc_repro.observations` 尚不存在得到2个collection error的有效RED；observation实现后14 passed，reward实现后聚焦35 passed；随后runtime invariant新增规格先得6个预期失败，修复后最终聚焦41 passed、完整290 passed，Ruff、mypy（19个源文件）、compileall均通过。手算确认paper observation `crj=.2±.2,u=1/3±1/3,tr=.1±.1`，corrected TR为`.2±.2`；插入等待令legacy TR保持0而corrected从0升至1.5；两机paper Uave `2/3±1/3`、standard utilization `1/3±0`；四reward的零/相等/90%边界符合锁定差异。首轮单提交为 `c63a2c5d45210b0e414d7984b49238b84c1105c8`，未推送。
158. Task 5 独立只读审查确认runtime字段/identity、全部observation公式、PROCESS与全interval horizon区别、未来作业纳入、population std、具名向量和reward零/相等分支均正确，但发现一项Important类型漏洞：Python中 `0.0 == 0` 会让float timeline/runtime-machine ID通过contiguous检查，fractional/bool/string last-machine引用也缺少先验整数校验，随后 `compute_observation` 可能以float索引list而崩溃。已要求原实施者先为timeline ID、runtime machine ID、last_machine_by_job的float/bool/string增加RED，再仅在runtime.validate中要求非bool int后做连续/范围校验，保持所有公式和API不变，amend同一Task 5提交并重跑5090全门禁。
159. Task 5 审查修复先得到9个非法ID用例失败的RED，实现非bool int校验后9个review-focused用例通过、Task 5聚焦50、完整套件299，5090上Ruff/mypy/compileall均通过；float/bool/string timeline ID、runtime-machine ID和last-machine引用现均在比较/索引前以清晰ValueError拒绝。提交amend为 `c78f512ecdf617f5d101ae4bd79eb991f5fd72b0`，仍是唯一Task 5提交且未推送。独立复审用19个非法ID探针验证原float索引崩溃已安全关停，合法ID的paper/standard utilization与reward零矩阵保持不变，Critical/Important/Minor全空、`Ready to proceed: Yes`，Task 5正式完成。
160. Task 6 首轮TDD在5090因 `smc_repro.rules` 尚不存在得到3个collection error的有效RED；实现immutable views与23条规则后，legacy 15 passed、paper 8 passed、聚焦32 passed、完整331 passed，Ruff、mypy（24个源文件）、compileall通过。64个decision index随机扫验证legacy机器{0,1}/作业{0,1,2}、paper机器{0,2}/作业{0,1,2}均覆盖，repeatable/decision-sensitive/action-separated/policy-separated均true；禁用import文本与AST检查均0违规。9 legacy、9 paper、5 classical的手算job/op/machine答案及唯一稳定名称全部记录于报告。首轮单提交为 `b158c5f56e679c4fc2c0c90d19a57b5fa83d9030`，未推送。
161. Task 6 独立只读审查确认全部公式、故意括号、B1/B2互换、classical tie、随机命名空间、无禁用import和精确`__all__`正确，但给出两项Important：冻结RuleContext未对instance_id/seed/index做严格类型校验且可别名可变jobs/pairs list；legacy/paper action dict lookup会把False当0、True/1.0当1并让unhashable泄漏TypeError。另有两项Minor测试问题：classical无突变断言为同对象自反，未覆盖全部规则；测试间裸import依赖pytest prepend模式，在`--import-mode=importlib`收集失败。已要求先补严格边界RED，再冻结/防御复制context、拒绝非bool整数action、对全部23规则做deep-snapshot无突变、重构共享fixture为import-mode独立，并把64+随机覆盖扩到每个随机组合；随后amend同一Task 6提交并重跑5090门禁。
162. Task 6 审查修复新增边界后正常/importlib聚焦均先得 `50 failed, 33 passed` 的有效RED；修复后两模式均83 passed、完整套件382 passed，5090上Ruff、mypy（24源文件）、compileall通过，禁用import grep为0、AST违规为空。RuleContext现要求非空string instance_id、非bool非负整数index/seed、将输入序列防御复制为tuple并校验精确view类型；legacy/paper action在dict lookup前统一拒绝全部非整数。deep snapshot覆盖9+9+5，无prepend依赖；每个随机组合扫128索引并覆盖全部候选。提交amend为 `629331f37cfe38f15e71359959ab386b9418ba7b`，仍是唯一Task 6提交、未推送。独立复审确认全部finding解决、数学/名称/API不变、Critical/Important/Minor全空、`Ready: Yes`，Task 6正式完成。
163. Task 7 多层TDD在5090依次得到：缺失environment的focused collection exit 2、contract/timeline各exit 1、step-layer 1个NotImplemented失败、contract-layer 16 failures，以及clock rollback/unknown profile回归；实现后context/candidate 6 passed、environment/transaction/maintenance/23-rule 52 passed、contract 16 passed、环境+CRN聚焦最终72、完整453，Ruff、mypy（26源文件）、compileall通过。23种策略均完成有效小实例；materialized固定实例的9 legacy+9 paper action各完成155决策，makespan全部记录于报告；CRN primary draw `0.5020328588015102` 在policy seed变化与1万次无关draw后不变。首轮提交 `6fd7d639af4af8bc54bc026dad7d36e740414a95`，未推送。
164. Task 7 独立只读审查确认candidate/ready-clock/tail append、tool-change、SETUP→PM→CM→PROCESS、profile维护/退化/高负载、CRN、final validation、RunContract及全部门禁总体正确，但发现两项Important：live timeline先提交、其余runtime字段后赋值，若后赋值异常会产生半提交；超大arrival `1e16` 下 nominal4×degradation1.01 的metadata期望4.04，但浮点interval实际duration4.0，validator仍判ok。另有Minor：contract SHA测试是自比较。已要求增加晚提交全状态rollback RED和大时间metadata RED；runtime强制exact mutable list并改为完整proposed runtime/StepResult预构建后一次reference swap；validator以明确abs/rel 1e-9校验metadata数值/成对/finite及duration乘积；contract测试改为独立hard-coded SHA，随后amend同一Task 7提交并重跑23策略、18 action与5090全门禁。
165. Task 7 审查修复先得environment 6 failures与validator `7 failed,1 passed`的有效RED；修复后exact list containers、完整proposed runtime/StepResult预构建、单次 `self.runtime=proposed_runtime` 发布，三类precommit异常均全状态零变化。validator要求nominal/degradation metadata完整成对、finite/numeric/nonbool/positive，并以rel/abs `1e-9`校验nominal与duration乘积；大时间 `1e16+4.04` 现事务性拒绝。RunContract使用独立430字节canonical vector和hard-coded SHA `927247183abb7fdbac6e97fc00e40a1f7128c020775c1d3a01da897c01957239`。首轮amend `a429248...` 后聚焦104、完整467，23策略及18 action保持有效。
166. Task 7 复审确认原三项finding清零，但发现Minor极端metadata：JSON scalar `10**1000` 传入 `math.isfinite` 会裸抛OverflowError。增加两项RED后，以受保护的单次数值转换捕获Overflow/Value/Type错误并返回invalid ValidationReport；最终提交 `434ee29f12ca368dd5c578bc2dd7b0be42cc1c43`，5090聚焦106、完整469，Ruff/mypy/compileall通过，transaction probes/23策略/18 action与contract SHA不变。最终独立复审Critical/Important/Minor全空、`Ready: Yes`，Task 7正式完成且未推送。
167. Task 8 按测试先行在5090得到4个collection error、exit 2 的有效RED，均因 `smc_repro.agents` 尚不存在；实现精确MLP、上下文、私有RNG回放、Double/vanilla target、双层value agent、strict checkpoint、Q-learning/SARSA、十状态离散器和joint reward后，首轮聚焦50、完整519，Ruff、mypy（32源文件）、compileall及CUDA probe通过。受限 `non-scientific functionality smoke` 只运行8个规定单元、每单元固定 `train/seed_000` 的3个唯一实例；仅对相同实例/seed做一次CPU确定性重播，Run A/B canonical JSON字节相同且SHA-256均为 `57104bb45b6f450c5d2ad6dc0075bdd89150f038b22dacee639ae886e5344283`，没有运行200×5训练、正式evaluation、聚合、绘图或结果声明。首轮提交为 `56ab597ec84510471de636fc204d62187e0dee3a`，未推送。
168. Task 8 首轮独立只读审查复跑5090门禁并核对8×3上限，发现三项Important和一项Minor：checkpoint把CUDA RNG topology无条件用于network-only eval而破坏CPU/CUDA可移植性；lower context/value target静默接受fractional reward ID、错误shape广播与非有限gamma；agent smoke只在本机 `.git/info/exclude` 忽略；Transition/tabular的huge-int有限性检查泄漏裸异常。审查修复先得到严格边界 `46 failed,5 passed` 以及topology/huge-int `4 failed` 的RED，随后增加tracked精确ignore、统一reward ID和target shape/dtype/bool/finite校验、training resume严格但eval不恢复RNG且跨topology可移植、huge-int受控ValueError；首轮复修后聚焦117、完整586并amend为 `3a5cabc...`。
169. 原审查者复审首轮复修时在真实RTX发现新的交叉场景Important：`DualLayerValueAgent(device="cpu")` 内部 `torch.manual_seed` 会改变全局CUDA RNG，checkpoint临时CPU preflight agent也会使成功eval和拒绝load污染调用者CUDA RNG，既有测试漏掉该情形。按审查新增CPU agent构造、eval load、拒绝training load三类回归，在5090先得 `3 failed,56 deselected` 的RED；随后无论selected device为何，只要CUDA可用就让 `fork_rng` 覆盖所有CUDA devices，三类用例GREEN为 `3 passed,56 deselected`，拒绝load同时保持目标agent零mutation。
170. Task 8 最终提交amend为 `863027cf015ae563b6baced766315305d116cac7`，仍是相对Task 7的唯一一个提交且未推送。最终5090 targeted 4、聚焦120、完整589全部通过，Ruff、mypy（32源文件）、compileall和非episodic CUDA evidence全绿；CPU/CUDA全局RNG均保持且same-seed参数一致。独立原子写入探针确认正常路径fsync=1/replace=1且SHA匹配，注入replace失败时旧文件保留、临时文件为0。tracked ignore在clean-clone探针生效，原3项Important和1项Minor均保持解决，smoke未重跑或扩量。
171. Task 8 最终独立复审确认 `863027c` 的Critical/Important/Minor均为0、`Ready: Yes`；12个checkpoint、严格8×3 smoke、Run A/B字节一致及无正式科学实验边界均已复核。随后主控制器完整阅读Task 9 brief及权威计划3267–3726行，确认Task 9分Phase A实现/独立审查与Phase B真实clean clone/P00–P15/最终HEAD复验两段执行，Task 9拥有 `all.md`，且实现期间不得push。
172. Task 9 Phase A 从已独立批准的Task 8提交 `863027cf015ae563b6baced766315305d116cac7` 开始；实施者完整读取Task 9 brief、权威计划3267–3726行和现有 `all.md`，核对工作树除主控制器移交的本日志外无跟踪改动，并确认本阶段只允许测试先行、5090上的单元/质量门禁和唯一实现提交，明确禁止真实全仓P00–P15、真实clean-worktree gate、本机4070、环境安装、正式科学实验与push。随后先创建 `test_preflight.py`，覆盖canonical/NaN、固定P00–P15顺序与首次失败落盘、profile/method/seed、bank containment、invalid schedule先于metrics、epsilon 0、确定性字节、generated artifact、missing checkpoint，以及tiny Git仓库的tracked-only/non-ASCII/dirty/failure/cleanup行为；此时两个实现模块尚未创建，准备在真实5090 staging取得预期RED。
173. Task 9 Phase A 将当前 `src`、`tests`、`configs`、`pyproject.toml` 传至真实5090的独立英文staging `C:\Users\LXT\smc_phase2_task9_20260820\original_repro`，以远端固定Python、显式 `PYTHONPATH`、`PYTHONHASHSEED=0`、`CUBLAS_WORKSPACE_CONFIG=:4096:8` 运行 `pytest -q tests\test_preflight.py`；collection按预期因 `smc_repro.scripts.clean_worktree_gate` 尚不存在得到1个error、exit 1，构成实现前有效RED，未触发任何GPU episode、真实preflight或clean gate。
174. Task 9 Phase A 创建两个实现模块、README双平台命令及tracked `artifacts/preflight/` 忽略规则后，5090聚焦测试首轮为13 passed；首轮静态门禁暴露11项可机械修复的Ruff问题和3项mypy问题，compileall已通过，修正导入、属性访问、明确转换/返回类型后复跑为13 passed、Ruff通过、mypy对34个源文件通过、compileall通过。随后完整回归首轮得到 `600 passed, 2 failed`：两项失败均为轻量staging最初只传了src/tests/config/pyproject而缺少既有 `docs/audit/legacy_tracked_manifest.json` 与 `original_repro/.gitignore`，分别触发既有manifest存在性断言和Task 8 ignore存在性断言；Ruff/mypy/compileall仍全绿。该失败被判定为staging构造缺件而非实现回归，未修改测试或产品语义，准备补传原跟踪文件后在5090重跑完整套件。
175. Task 9 首次补传文件后完整回归仍为 `600 passed, 2 failed`，进一步定位出两项独立问题：轻量staging缺少真实的“仓库根/扩刊/original_repro”层级，令既有legacy测试把 `C:\Users\LXT` 误判为repo root；同时将项目内Task 8精确ignore扩成整个preflight目录破坏了其“相邻文件不应被该项目内规则忽略”的锁定测试。未修改既有测试；实施者恢复项目内精确 `artifacts/preflight/agent_smoke/`，改由仓库根tracked `.gitignore` 忽略正式 `扩刊/original_repro/artifacts/preflight/`，并从已验证Task 8真实Git staging复制出结构完整的 `C:\Users\LXT\smc_phase2_task9_full_20260820` 后仅覆盖Task 9文件。5090完整复跑最终为 `602 passed in 17.06s`（计时器18.369秒、0 failed），Ruff全src/tests通过、mypy对34个源文件通过、compileall通过；该运行仍仅含unit/tiny-repo gate，未执行真实P00–P15、真实clean-worktree gate、bank全验证或任何新增科学episode。
176. Task 9 Phase A 最终实现核对确认：clean-worktree gate解析真实Git根、拒绝dirty、用当前HEAD建立detached worktree、只用显式Python并设置worktree `PYTHONPATH`/两项确定性环境变量、顺序运行pytest/Ruff/mypy/compileall与tracked legacy audit、为每条命令记录exit/duration/stdout/stderr SHA，并在任何失败或清理失败时先持久化failed report且finally移除worktree；preflight以P00–P15固定顺序首错即停并先落盘，canonical科学payload排除时间戳/耗时而包含固定profile/method/seed计划、contracts、bank/environment、完整episode/checkpoint/epsilon/determinism/generated-artifact证据，Q-learning/SARSA明确采用已审计 `legacy_joint` 协议，deep replay seed为agent seed+1，所有环境流使用规定offset。最终代码变更后5090再次完整复跑为 `602 passed in 17.23s`（计时器18.6533963秒）、Ruff/mypy(34源文件)/compileall全绿，两个CLI `--help` 均exit 0；本地 `git diff --check`、legacy三目录零diff、正式preflight/checkpoint ignore匹配及无已跟踪generated artifacts均通过。完整Phase A RED/GREEN/边界写入ignored `task-9-report.md`；真实P00–P15、真实clean gate、1540全验证、任何正式科学实验和push仍明确未运行，现准备唯一实现提交和独立审查。
177. Task 9 Phase A 首轮独立审查完整读取 `dd9bb0354e9302b9a2ba7a630168ca393f713348` 后给出 `Critical=1, Important=2, Minor=1, Ready: No`；实施者按receiving-code-review逐项回查，确认四项均与代码现实一致：非法 `--output` 在P00拒绝后仍会被failed-report handler覆盖；P01只检查部分字段且丢弃capture commit；P11把next replay sample“exact”缩减为仅state；clean gate默认命令/显式解释器和cleanup-command失败虽已实现但未被测试锁定。范围保持仅Phase A修复，禁止进入真实P00–P15/真实clean gate。
178. Task 9 审查修复先新增16项回归，覆盖已提交的code/code1/code2受保护victim与repo外victim字节/clean status不变、preflight子目录symlink外逃、旧但canonical capture commit、schema999/null或大写commit/cuda false/版本与device失配、保持state相同而分别篡改Transition其余五字段、默认4质量命令+audit的顺序/cwd/显式Python/PYTHONPATH/两环境变量，以及注入 `git worktree remove` 失败后failed report/raise/物理零残留。真实5090首轮为 `14 failed, 15 passed`：受保护/外部输出确实被JSON覆盖、symlink外部目标被创建，五个replay变体因完整比较helper不存在失败；环境支撑文件补传后7项environment用例均因strict helper尚不存在失败，构成逐finding有效RED。两项clean-gate默认/cleanup注入测试在既有实现上已通过，证明Minor为测试缺口而非当前行为缺陷。
179. Task 9 审查修复实现所有public CLI路径在构造runner/任何报告写入前的lexical+resolved+containment校验：bank/reference/environment固定到repo受控位置，preflight root及任意子路径symlink不得外逃，非法output以明确 `no report was written` 的 `PreflightInputError` 非零终止；因此受保护/跟踪/外部原字节及Git clean状态保持。P01现要求schema1精确字段集、canonical 40-hex capture commit（仅作provenance而不等同live HEAD）、cuda_available严格true、固定bank/env变量、ISO capture时间、合法capability/arches/sm_120/14.0、严格packages和Python/torch/CUDA/platform/device live匹配，并把capture commit纳入无运行时间戳的确定性evidence。P11现对每个采样Transition的state/action/reward/next_state/reward_id/done完整primitive做canonical字节比较后才声称相等并记录sample SHA。5090审查聚焦最终 `29 passed in 7.06s`，完整回归 `618 passed in 20.14s`（计时器21.5304152秒），Ruff全src/tests、mypy 34源文件、compileall全绿；仍未执行真实P00–P15、真实clean gate、bank全验证、科学episode或push。
180. Task 9 Phase A 审查修复提交前再次在5090结构完整staging执行非真实关卡验证：`test_preflight.py` 为 `29 passed in 7.20s`，完整套件为 `618 passed in 18.38s`，Ruff全src/tests为 `All checks passed!`，mypy为34个源文件零问题，compileall exit 0；两个CLI `clean_worktree_gate --help` 与 `preflight --help` 均exit 0。随后本地只读保护核验确认相对Task 8基线 `863027cf015ae563b6baced766315305d116cac7` 的 `code/`、`code1/`、`code2/` 零diff，Task 9生成报告/产物零tracked文件，`git diff --check` exit 0；下一步只把本次三处审查修复和累计 `all.md` 暂存，并用 `git commit --amend --no-edit` 更新既有Task 9唯一提交，不新增提交、不push。全程仍未运行真实P00–P15、真实clean-worktree gate、1540全验证或任何科学episode。
181. Task 9 Phase A 审查修复首次amend得到 `e8c62395d0f8aed866bcb300d0c0161ad045a98e`，提交主题仍精确为 `test: add clean-clone and end-to-end preflight gates`；该操作是在Task 8基线之上改写同一个Task 9提交而非新增提交。为让本条实际操作记录也进入累计日志，随后仅再次amend同一提交以纳入本条，不运行任何新关卡、不进入Phase B、不push；最终提交SHA由ignored Task 9报告及控制器交接记录保存。
182. Task 9 Phase A 第二轮独立复审在 `0dc194c5c8f1663d4bbf478322f48a41faad25b2` 仍发现1项Critical与1项Minor：若仓库内 `artifacts/preflight` 本身是指向受保护 `code/` 的symlink/junction，既有校验会把受控根与output同时解析到repo内部而错误放行，P00因dirty失败后的failed-report handler可覆盖tracked victim；默认clean-gate测试也只锁了命令名称而未锁完整argv及audit `--scope tracked`。实施者按receiving-code-review/systematic-debugging/TDD核对数据流，首个诊断输入因仍用默认report文件名只证明进入P00而未命中victim，修正为symlink下同名tracked victim后，真实5090单测得到 `1 failed, 29 deselected`：`code/victim.json` 的 `TRACKED ORIGINAL` 字节被JSON failed report覆盖，构成有效RED。范围继续严格限制为Phase A最小修复，不运行真实P00–P15、真实clean gate、1540全验证、科学episode或push。
183. Task 9 第二轮复审最小修复为：在任何runner构造/报告写入前，从已解析repo root开始对bank/reference/environment/preflight root/output的每个已存在路径组件执行 `lstat`，拒绝POSIX/Windows symlink以及Windows `FILE_ATTRIBUTE_REPARSE_POINT`（含junction），并额外要求preflight root解析后的路径与受控lexical canonical路径精确相等；因此仓库内部重定向也不能通过containment校验。新增internal-code-symlink回归先RED后为 `1 passed, 29 deselected`，victim字节与运行前Git状态均保持；clean-gate默认命令测试现锁定pytest/Ruff/mypy/compileall/audit五条完整argv与顺序、显式解释器、cwd、PYTHONPATH、两项确定性环境变量及audit末尾 `--scope tracked`，单测同为 `1 passed, 29 deselected`。两项组合首次运行被外层30秒限制以exit 124终止、没有形成测试结论，扩大有界超时后正常通过。最终真实5090非真实关卡验证为focused `30 passed in 7.77s`、完整 `619 passed in 18.87s`、Ruff全src/tests通过、mypy 34源文件零问题、compileall exit 0；本地 `git diff --check` 与相对Task 8基线的 `code/`、`code1/`、`code2/` 零diff。仍未运行真实P00–P15、真实clean-worktree gate、1540全验证、科学episode或push。
184. Task 9 第二轮复审修复首次amend得到 `323935b0e26a13d2d5dce5a76adc445ca11c7f74`，主题仍精确为 `test: add clean-clone and end-to-end preflight gates`，仍是Task 8基线之上的唯一Task 9提交。为将本条实际提交动作纳入累计日志，随后只再次amend同一提交以加入本条，不改变产品/测试语义、不重跑或启动真实关卡、不进入Phase B、不push；最终SHA写入ignored Task 9报告并交接给控制器。
185. Task 9 Phase A 最终独立复审对 `1bccdedd119f2e443f9630453078e5541bfb8c28` 给出Critical/Important/Minor均为0、`Ready for Phase B: Yes`；复审确认仓库内symlink/reparse绕过及default command完整argv缺口均已解决。控制器据此授权严格进入Phase B。实施者重新完整读取Task 9 brief、权威计划3267–3726行以及executing-plans/using-git-worktrees/verification-before-completion规则，确认正式范围仅为真实clean clone上的质量套件、hardware、1540 verifier、固定P00–P15及clean-worktree gate，禁止540正式方法循环、formal sweep、聚合、统计、图表、外部数据、GNN/PPO及push。本地隔离核验显示普通独立分支 `preexperiment-readiness-20260820`，Git dir/common dir均为仓库 `.git`，非submodule，起始HEAD为 `1bccdedd119f2e443f9630453078e5541bfb8c28` 且 `git status --short` 为空；按brief不另建本地worktree，而以本地bundle在5090创建真实Git clone作为正式隔离执行环境。
186. 为Phase B生成本地完整Git bundle `task9-phaseb-1bccded.bundle`，bundle verify确认包含完整历史及分支ref `1bccdedd119f2e443f9630453078e5541bfb8c28`，文件535081字节、SHA-256 `b4fc2b407f1d019a70a480fd20140f6043e298c6c3654e55e2a875b5a37ecefb`；另将仅有忽略的 `artifacts/banks/materialized/` 与Task 8 `artifacts/preflight/agent_smoke/` 打包为9505823字节、SHA-256 `9a51f26c3e3c87122dfa6d07e7e32d3503a72a430b74e7bc9c704ebc48b22648` 的archive。传至5090后以portable Git从bundle创建真实clone `C:\Users\LXT\smc_task9_phaseb_clean_1bccded_20260820`，非synthetic index；复制ignored产物前后HEAD均为 `1bccdedd119f2e443f9630453078e5541bfb8c28` 且status计数均0。clone内materialized bank为1540个gzip加1个manifest，materialized与tracked release manifest SHA均为固定 `68a2fd2420a8710743b28db1b234b69dc2ecf96046d14950abac22cee7cd1515`；Task 8 smoke为18文件，`git ls-files` 对materialized/preflight生成目录计数0。clone过程只有portable Git缺少templates目录的warning，不影响clone/HEAD/status，记为Phase B deviation候选并继续保留证据。
187. 在上述genuine clone按Step 10固定顺序、显式 `C:\Users\LXT\smc_gate1_env\Scripts\python.exe`、clone `PYTHONPATH`、`PYTHONHASHSEED=0`、`CUBLAS_WORKSPACE_CONFIG=:4096:8` 执行Phase B第一段真实门禁：full pytest为 `619 passed in 19.65s`、命令计时21.024209秒；Ruff全src/tests为 `All checks passed!`、0.195034秒；mypy为34个源文件零问题、46.621531秒；compileall exit 0、0.281081秒。hardware exit 0、3.546172秒，报告Python 3.11.16、Windows 10.0.26200、torch 2.10.0+cu128、CUDA runtime 12.8、`cuda_available=true`、NVIDIA GeForce RTX 5090、compute capability 12.0、compiled arches含 `sm_120`、CUDA tensor结果14.0。随后1540 verifier exit 0、18.352556秒：expected=verified=1540、ok=true、generated/reference manifest SHA均为固定 `68a2fd2420a8710743b28db1b234b69dc2ecf96046d14950abac22cee7cd1515`；六项结束后remote clone Git status计数仍0。
188. 第一次真实P00–P15在同一genuine clone以 `cuda:0` 启动，HEAD为 `1bccdedd119f2e443f9630453078e5541bfb8c28`；命令10.979630秒后exit 1，按首错即停写出 `status=failed` 报告，报告git_commit正确、gate计数7，P00–P05通过而P06失败，最后消息为 `environment_metadata_path must be repository-relative`。完整stack显示 `_PreflightRunner.p06 -> contract -> build_run_contract -> _repository_relative_posix_path`，调用方把runner内已解析的绝对环境metadata路径传给只接受repository-relative路径的contract builder；failed report已在raise前持久化。依照systematic-debugging与Phase B stop rule，立即停止后续P00–P15及real clean gate，不进入docs evidence commit，先只做根因追踪、保留失败证据并向控制器回报。
189. 控制器复核failed report与调用链后确认单一根因成立并授权回到TDD：新增真实 `_PreflightRunner.contract` 边界回归，以绝对runtime环境路径构造runner但要求RunContract中保存literal POSIX repo-relative `扩刊/docs/audit/environment_5090_resolved.json`。首轮5090测试因夹具误写不存在的 `configs/profiles/paper_repro.yaml` 在到达被测边界前失败，属于无效RED；修正为tracked `configs/paper_repro.yaml` 后得到目标 `1 failed, 30 deselected`，stack精确显示absolute Path被builder拒绝。最小实现只在caller把 `self.environment_metadata.relative_to(self.repo_root)` 传给 `build_run_contract`，未放宽RunContract validator；目标GREEN为 `1 passed, 30 deselected in 4.92s`。5090 staging随后fresh focused为 `31 passed in 8.22s`、full为 `620 passed in 20.96s`，Ruff全src/tests通过、mypy 34源文件零问题、compileall exit 0。本次修复后严格不续跑genuine clone/P00–P15，先amend同一Phase A实现提交并请求独立复审。
190. Phase B P06 caller修复首次amend得到 `07c271df99acf1d536489a43357898ac7e639d2f`，提交主题仍精确为 `test: add clean-clone and end-to-end preflight gates`，相对Task 8仍只有一个Task 9实现提交且尚无docs evidence commit。为使本条实际amend动作也进入累计日志，随后仅再次amend同一提交纳入本条；产品语义不再变化，不更新或续跑genuine clone，不运行真实P00–P15/clean gate，不push，最终amended SHA交由独立复审。
